<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sampleapp.dao.OrderDao">

    <resultMap id="orderResultMap" type="com.example.sampleapp.model.Order">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="orderDate" column="order_date"/>
        <result property="status" column="status"/>
        <result property="totalAmount" column="total_amount"/>
    </resultMap>

    <resultMap id="orderItemResultMap" type="com.example.sampleapp.model.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="unitPrice" column="unit_price"/>
    </resultMap>

    <select id="findByUserId" parameterType="int" resultMap="orderResultMap">
        SELECT id, user_id, order_date, status, total_amount
        FROM orders
        WHERE user_id = #{userId}
        ORDER BY order_date DESC
    </select>

    <select id="findById" parameterType="int" resultMap="orderResultMap">
        SELECT id, user_id, order_date, status, total_amount
        FROM orders
        WHERE id = #{id}
    </select>

    <insert id="insertOrder" parameterType="com.example.sampleapp.model.Order"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, order_date, status, total_amount)
        VALUES (#{userId}, NOW(), 'PENDING', #{totalAmount})
    </insert>

    <insert id="insertOrderItem" parameterType="com.example.sampleapp.model.OrderItem"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_items (order_id, product_id, quantity, unit_price)
        VALUES (#{orderId}, #{productId}, #{quantity}, #{unitPrice})
    </insert>

    <update id="updateOrderStatus">
        UPDATE orders
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <select id="findItemsByOrderId" parameterType="int" resultMap="orderItemResultMap">
        SELECT id, order_id, product_id, quantity, unit_price
        FROM order_items
        WHERE order_id = #{orderId}
    </select>

</mapper>
