<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.testsampleapp.dao.OrderItemDao">

    <resultMap id="orderItemResultMap" type="com.example.testsampleapp.model.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="productName" column="product_name"/>
        <result property="productCategory" column="product_category"/>
    </resultMap>

    <select id="findByOrderId" parameterType="int" resultMap="orderItemResultMap">
        SELECT oi.id, oi.order_id, oi.product_id, oi.quantity, oi.unit_price,
               p.name AS product_name, p.category AS product_category
        FROM order_items oi
        JOIN products p ON p.id = oi.product_id
        WHERE oi.order_id = #{orderId}
    </select>

</mapper>
